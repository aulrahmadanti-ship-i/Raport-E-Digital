<!DOCTYPE html>
<html lang="id">
<head>
  <%- include('partials/head') %>
</head>
<body class="min-h-screen flex items-center justify-center bg-slate-bg">
  <div class="text-center px-6" data-aos="fade-up">
    <div class="w-24 h-24 bg-sma-grey/10 rounded-full flex items-center justify-center mx-auto mb-6">
      <i class="fa-solid fa-triangle-exclamation text-4xl text-sma-grey"></i>
    </div>
    <h1 class="text-6xl font-extrabold text-gray-800 mb-2"><%= error.status || 500 %></h1>
    <h2 class="text-xl font-semibold text-gray-600 mb-4"><%= message %></h2>
    <p class="text-sm text-gray-400 mb-8 max-w-md mx-auto">Halaman yang Anda cari tidak ditemukan atau terjadi kesalahan pada server.</p>
    <a href="/" class="inline-flex items-center gap-2 bg-sma-grey hover:bg-sma-dark text-white px-6 py-3 rounded-xl font-semibold transition-all shadow-lg">
      <i class="fa-solid fa-home"></i> Kembali ke Beranda
    </a>
    <% if (error.stack && process.env.NODE_ENV !== 'production') { %>
      <pre class="mt-8 text-left text-xs text-gray-400 bg-gray-50 p-4 rounded-xl overflow-auto max-w-2xl mx-auto"><%= error.stack %></pre>
    <% } %>
  </div>
  <script src="https://unpkg.com/aos@2.3.4/dist/aos.js"></script>
  <script>AOS.init({ duration: 600, once: true });</script>
</body>
</html>

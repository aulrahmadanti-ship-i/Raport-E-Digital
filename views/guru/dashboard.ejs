<!DOCTYPE html>
<html lang="id">
<head>
  <%- include('../partials/head') %>
</head>
<body class="min-h-screen flex bg-slate-bg font-sans">

  <%- include('../partials/sidebar') %>

  <div class="flex-1 flex flex-col h-screen overflow-hidden">
    <header class="bg-white lg:bg-transparent p-4 lg:p-0 flex justify-between items-center lg:justify-end lg:pr-8 lg:pt-6">
      <button onclick="toggleSidebar()" class="lg:hidden text-gray-600 text-xl p-2 hover:bg-gray-100 rounded-lg">
        <i class="fa-solid fa-bars"></i>
      </button>
      <div class="flex items-center gap-4">
        <button class="w-10 h-10 rounded-full bg-white shadow-soft flex items-center justify-center text-gray-500 hover:text-sma-grey relative">
          <i class="fa-regular fa-bell"></i>
        </button>
      </div>
    </header>

    <main class="flex-1 overflow-y-auto p-4 lg:p-8">
      <div class="max-w-7xl mx-auto space-y-6">

        <!-- Welcome -->
        <div class="bg-gradient-to-r from-sma-grey to-slate-600 rounded-3xl shadow-premium p-8 relative overflow-hidden text-white">
          <div class="absolute top-0 right-0 w-64 h-64 bg-white opacity-5 rounded-full -mr-16 -mt-16 blur-2xl"></div>
          <p class="text-slate-200 mb-1 font-medium">
            <%= new Date().toLocaleDateString('id-ID', { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' }) %>
          </p>
          <h2 class="text-3xl font-bold mb-2 text-balance">Selamat Datang, <%= user.nama_lengkap %>!</h2>
          <p class="text-sm text-slate-300 max-w-lg">Kelola nilai dan rapor siswa Anda dengan mudah melalui portal ini.</p>
        </div>

        <!-- Stats -->
        <div class="grid grid-cols-1 sm:grid-cols-4 gap-6">
          <div class="bento-card bg-white p-6 rounded-3xl shadow-soft flex items-center justify-between border border-gray-100">
            <div>
              <p class="text-gray-400 text-xs font-semibold uppercase tracking-wider mb-1">Total Siswa</p>
              <h3 class="text-2xl font-bold text-gray-800"><%= typeof totalSiswa !== 'undefined' ? totalSiswa : 0 %></h3>
            </div>
            <div class="w-12 h-12 rounded-2xl flex items-center justify-center text-xl bg-blue-50">
              <i class="fa-solid fa-users text-blue-500"></i>
            </div>
          </div>
          <div class="bento-card bg-white p-6 rounded-3xl shadow-soft flex items-center justify-between border border-gray-100">
            <div>
              <p class="text-gray-400 text-xs font-semibold uppercase tracking-wider mb-1">Mata Pelajaran</p>
              <h3 class="text-2xl font-bold text-gray-800"><%= typeof totalMapel !== 'undefined' ? totalMapel : 0 %></h3>
            </div>
            <div class="w-12 h-12 rounded-2xl flex items-center justify-center text-xl bg-green-50">
              <i class="fa-solid fa-book text-green-500"></i>
            </div>
          </div>
          <div class="bento-card bg-white p-6 rounded-3xl shadow-soft flex items-center justify-between border border-gray-100">
            <div>
              <p class="text-gray-400 text-xs font-semibold uppercase tracking-wider mb-1">Nilai Terisi</p>
              <h3 class="text-2xl font-bold text-gray-800"><%= typeof nilaiTerisi !== 'undefined' ? nilaiTerisi : 0 %></h3>
            </div>
            <div class="w-12 h-12 rounded-2xl flex items-center justify-center text-xl bg-orange-50">
              <i class="fa-solid fa-check-double text-orange-500"></i>
            </div>
          </div>
          <div class="bento-card bg-white p-6 rounded-3xl shadow-soft flex items-center justify-between border border-gray-100">
            <div>
              <p class="text-gray-400 text-xs font-semibold uppercase tracking-wider mb-1">Kelas</p>
              <h3 class="text-2xl font-bold text-gray-800"><%= typeof totalKelas !== 'undefined' ? totalKelas : 0 %></h3>
            </div>
            <div class="w-12 h-12 rounded-2xl flex items-center justify-center text-xl bg-indigo-50">
              <i class="fa-solid fa-school text-indigo-500"></i>
            </div>
          </div>
        </div>

        <!-- Quick Actions -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <a href="/guru/input-nilai" class="bento-card bg-white p-6 rounded-3xl shadow-soft border border-gray-100 flex items-center gap-4">
            <div class="w-14 h-14 bg-green-50 rounded-2xl flex items-center justify-center">
              <i class="fa-solid fa-keyboard text-2xl text-green-600"></i>
            </div>
            <div>
              <h3 class="font-bold text-gray-800 text-lg">Input Nilai Siswa</h3>
              <p class="text-sm text-gray-500">Masukkan nilai per kelas dan mata pelajaran</p>
            </div>
            <i class="fa-solid fa-arrow-right text-gray-300 ml-auto"></i>
          </a>
          <a href="/guru/input-nilai" class="bento-card bg-white p-6 rounded-3xl shadow-soft border border-gray-100 flex items-center gap-4">
            <div class="w-14 h-14 bg-blue-50 rounded-2xl flex items-center justify-center">
              <i class="fa-solid fa-file-excel text-2xl text-blue-600"></i>
            </div>
            <div>
              <h3 class="font-bold text-gray-800 text-lg">Import Excel</h3>
              <p class="text-sm text-gray-500">Upload file nilai dari template Excel</p>
            </div>
            <i class="fa-solid fa-arrow-right text-gray-300 ml-auto"></i>
          </a>
        </div>

      </div>
    </main>
  </div>

</body>
</html>
